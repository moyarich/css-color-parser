---
id: "monaco-unresolved-invalid"
label: "Unresolved / Invalid Colors"
group: "Editor"
eyebrow: "Editor integration"
title: "Unresolved / Invalid Colors"
description: "Context-dependent and malformed values that should remain unresolved."
toc: true
---

import MonacoExtraction from "../MonacoExtraction"
import source from "./source"
import sourceCode from "./source.ts?raw"

## What this example covers

Context-dependent and malformed values that should remain unresolved.

## Extraction behavior

The live result is produced from the same editable Monaco model used by `provideDocumentColors()`.

<ExamplePage
  {...frontmatter}
  source={sourceCode}
  preview={<MonacoExtraction source={source} title={frontmatter.title} />}
>

## What this example covers

Context-dependent and malformed values that should remain unresolved.

## Extraction behavior

The live result is produced from the same editable Monaco model used by `provideDocumentColors()`.

</ExamplePage>
