---
id: "monaco-source-extraction"
label: "Source Extraction"
group: "Editor"
eyebrow: "Editor integration"
title: "Source Extraction"
description: "Multiple colors, gradients, offsets, and UTF-16 source positions."
toc: true
---

import MonacoExtraction from "../MonacoExtraction"
import source from "./source"
import sourceCode from "./source.ts?raw"

## What this example covers

Multiple colors, gradients, offsets, and UTF-16 source positions.

## Extraction behavior

The live result is produced from the same editable Monaco model used by `provideDocumentColors()`.

<ExamplePage
  {...frontmatter}
  source={sourceCode}
  preview={<MonacoExtraction source={source} title={frontmatter.title} />}
>

## What this example covers

Multiple colors, gradients, offsets, and UTF-16 source positions.

## Extraction behavior

The live result is produced from the same editable Monaco model used by `provideDocumentColors()`.

</ExamplePage>
