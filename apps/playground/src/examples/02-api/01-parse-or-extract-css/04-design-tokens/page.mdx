---
id: "design-token-extraction"
label: "Design tokens"
group: "API"
parent: "Parse or extract CSS"
eyebrow: "extractCssColors usage"
title: "Extract design tokens from CSS"
description: "Find colors in CSS custom properties and turn the matches into a usable token inventory."
---

import Example from "./DesignTokenExtraction"
import source from "./DesignTokenExtraction.tsx?raw"

## Why this is useful

`extractCssColors()` gives exact values and source ranges. Those matches can be combined with surrounding CSS syntax to build token inventories, migration tools, documentation, or design-system audits.

<ExamplePage {...frontmatter} source={source}>
  <Example />
</ExamplePage>
